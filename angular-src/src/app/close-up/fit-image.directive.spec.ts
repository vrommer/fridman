import { FitImageDirective } from './fit-image.directive';

describe('FitImageDirective', () => {
  it('should create an instance', () => {
    const directive = new FitImageDirective();
    expect(directive).toBeTruthy();
  });
});
