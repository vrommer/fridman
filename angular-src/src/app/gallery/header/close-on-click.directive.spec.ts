import { CloseOnClickDirective } from './close-on-click.directive';

describe('CloseOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseOnClickDirective();
    expect(directive).toBeTruthy();
  });
});
